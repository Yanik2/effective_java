create table sedan (
                       id serial primary key,
                       brand varchar(255) not null
);

create table van (
                     id serial primary key ,
                     brand varchar(255) not null
);

create table truck (
                       id serial primary key ,
                       brand varchar(255) not null
);